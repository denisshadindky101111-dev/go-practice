# Task 1

## Условие

Напишите программу, в которой объявлена целочисленная переменная `i` со
значением `20`. Присвойте значение `i` переменной с плавающей запятой `f`.
Выведите значения `i` и `f`.

## Запуск

```bash
make run
```
